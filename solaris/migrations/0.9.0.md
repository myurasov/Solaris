---
to_version: 0.9.0
from_version: 0.8.1
title: "Add release skill and formalize memory/instructions.md as operating memory"
breaking: false
revertible: true
estimated_duration: "<1 minute"
touches: []
---

- [Summary](#summary)
- [Pre-flight checks](#pre-flight-checks)
- [Migrate](#migrate)
- [Validate](#validate)
- [Revert](#revert)

## Summary

0.9.0 is a framework-only release: it adds the `release` skill and formalizes `memory/instructions.md` as
the Solaris operating memory (terse, timestamped cross-project lessons and user preferences loaded every
session). No ai-pack schema or file layout changed; existing projects need no file edits.

## Pre-flight checks

- [ ] `ai/manifest.json` exists and is valid JSON.
- [ ] The framework is at 0.9.0 (`uv run -m solaris.tools.version current`).

## Migrate

1. **Stamp the version:** `uv run -m solaris.tools.version set --dir <project> 0.9.0`.

No other changes required - this is a marker migration.

## Validate

- [ ] `ai/manifest.json` `framework_version` is `0.9.0`.

## Revert

1. `uv run -m solaris.tools.version set --dir <project> 0.8.1`.
