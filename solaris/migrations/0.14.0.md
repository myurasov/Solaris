---
to_version: 0.14.0
from_version: 0.13.0
title: "Add the remote-footprint rule (~/.solaris/<component>/) to core and the engineer template"
breaking: false
revertible: true
estimated_duration: "<1 minute"
touches:
  - ai/engineer.agent.md
---

- [Summary](#summary)
- [Pre-flight checks](#pre-flight-checks)
- [Migrate](#migrate)
- [Validate](#validate)
- [Revert](#revert)

## Summary

0.14.0 adds one always-on operating rule to core and to the ai-pack `engineer.agent.md` template (rev 15):
the **remote footprint** rule - everything Solaris installs on a remote host (services, tools, config,
model/data caches) lives under `~/.solaris/<component>/` so it is discoverable, inventoriable, and removable
in one place; every installer ships with an uninstaller, and each install (host + path) is recorded in the
relevant `resources.md`. The bundled `visual-qa` plugin also advances to 0.2.0 (serving lifecycle scripts,
instance registry, native video + `watch` tool); projects with the plugin attached migrate it separately via
the plugin's own `migrations/0.2.0.md` (driven by `update-project` / `install-plugin`). Content-only; no
ai-pack schema change.

## Pre-flight checks

- [ ] `ai/manifest.json` exists and is valid JSON.
- [ ] The framework is at 0.14.0 (`uv run -m solaris.tools.version current`).

## Migrate

1. **Fast-forward the engineer agent:** `uv run -m solaris.tools.revs ff --dir <project>` picks up
   `engineer.agent.md` rev 15 when the project copy is unmodified. If it reports a merge, add the
   "Remote footprint" bullet from the template's "Project modes" section by hand and bump the file's rev.
2. **Stamp the version:** `uv run -m solaris.tools.version set --dir <project> 0.14.0`.
3. **(If visual-qa is attached)** apply the plugin's `migrations/0.2.0.md` via `install-plugin` (migrate).

## Validate

- [ ] `ai/manifest.json` `framework_version` is `0.14.0`.
- [ ] `ai/engineer.agent.md` contains the "Remote footprint" bullet.
- [ ] `uv run -m solaris.tools.version check --dir <project>` passes.

## Revert

1. Restore the previous `ai/engineer.agent.md` (rev 14) from git history.
2. `uv run -m solaris.tools.version set --dir <project> 0.13.0`.
