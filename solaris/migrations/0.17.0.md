---
to_version: 0.17.0
from_version: 0.16.0
title: "Bundled aisee plugin: eyes via the AISee service (marker migration)"
breaking: false
revertible: true
estimated_duration: "<1 minute"
touches:
  - ai/manifest.json
---

- [Summary](#summary)
- [Pre-flight checks](#pre-flight-checks)
- [Migrate](#migrate)
- [Validate](#validate)
- [Revert](#revert)

## Summary

Marker migration: 0.17.0 adds a new bundled plugin `plugins/aisee/` (knowledge-only "eyes"
for visual verification via the standalone AISee service - MCP preferred, REST/CLI
fallbacks). The plugin is opt-in per project via install-plugin; no existing ai-pack file
changes. This migration only records the framework version step.

## Pre-flight checks

- [ ] `ai/manifest.json` exists and is valid JSON.

## Migrate

1. Set `framework_version` to `0.17.0` in `ai/manifest.json`.

## Validate

- [ ] `ai/manifest.json` reads `"framework_version": "0.17.0"`.

## Revert

1. Restore the previous `framework_version` value in `ai/manifest.json`.
