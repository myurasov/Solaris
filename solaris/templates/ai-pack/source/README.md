# {{NAME}} - source

Application code lives here ({{TYPE}}). This is the project's **default workspace** (self-contained; see
`../ai/engineer.agent.md` Workspaces) and in local mode the git root. The engineer agent generates
and maintains this tree against [`../ai/spec.md`](../ai/spec.md). In remote-code mode this folder is removed
and the code lives on the host named in `../remote.json`.
