_Rev. 1_

<!-- Minimal pointer. Edit ai/developer.agent.md, not this file. -->

# {{NAME}} - Agent Instructions

Project **{{NAME}}** ({{TYPE}}, {{MODE}} mode). Generated by Solaris.

On every turn, load and follow:

1. [`ai/developer.agent.md`](ai/developer.agent.md) - the developer persona (with embedded commit + safety policies).
2. [`ai/spec.md`](ai/spec.md) and `ai/memory/*`.
3. Every `ai/<plugin>/` overlay: each `*.rule.md` (always-on) and `*.skill.md` (trigger-invoked).
4. `src/AGENTS.md` if present (local mode) as project rules; in remote-code mode the code lives at the host
   in `remote.json`.

When working under a Solaris checkout, framework tools are available as `uv run -m solaris.tools.<name>`.
